# Python dependencies.
#
# Verified with Python 3.8+ on torch 2.0.1+cu117 / torchvision 0.15.2.
# Lower bounds only, so the file also works on newer stacks.
#
#   pip install -r requirements.txt
#
# Note: inversefed/genmodels/stylegan2_io/op JIT-compiles two CUDA extensions
# (fused_bias_act, upfirdn2d) at import time, so nvcc must match the CUDA
# toolkit that PyTorch was built against.
torch>=2.0.1
torchvision>=0.15.2
numpy
pandas>=2.0
pyyaml
matplotlib
scikit-image
scikit-learn
scipy
tqdm
lpips
nevergrad>=0.5.0
opencv-python
pillow

# Optional, only needed for the DeiT global models (see README).
timm

# Optional, only needed to download the StyleGAN2 weights (see README).
gdown
